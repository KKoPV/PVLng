/**
 * DataTables can use cookies in the end user's web-browser in order to store
 * it's state after each change in drawing. What this means is that if the user
 * were to reload the page, the table should remain exactly as it was (length,
 * filtering, pagination and sorting). This is disabled by default.
 */
var DatatablesStateSave = false;
